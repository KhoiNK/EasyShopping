<div class="col-sm-9">
    <h1>Be a part of us</h1>
    <p>This is where introduction message place</p>
    <button class="btn btn-success btn-lg" style="text-align:center" data-toggle="modal" data-target="#apply">Apply Now</button>
    <div class="modal fade" id="apply" role="dialog">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Recruitment Requirement</h4>
                </div>
                <div class="modal-body">
                    <div class="alert alert-success" *ngIf="message">{{message}}</div>
                    <div class="alert alert-danger" *ngIf="error">{{error}}</div>
                    <label>
                        Bank Account:
                    </label>
                    <br />
                    <input type="text" class="form_input" [(ngModel)]="shipper.BankAccount" />
                    <br />
                    <div style="position:relative">
                        <label>
                            Deposit:
                        </label>
                        <i class="glyphicon glyphicon-info-sign" data-toggle="tooltip" title="With deposit your chance to find a order is higher" style="position:absolute"></i>
                    </div>
                    <input type="text" class="form_input" [(ngModel)]="shipper.Deposit" />
                </div>
                <div class="modal-footer">
                    <button class="btn btn-success" (click)="SaveChange()">Apply</button>
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button hidden id="messageModal" data-toggle="modal" data-target="#modalMessage"></button>
                </div>
            </div>
        </div>
        <div class="modal fade" id="modalMessage" role="dialog" *ngIf="shipper">
            <div class="modal-content">
                <div class="modal-dialog modal-lg">
                    <div class="modal-dialog">
                        Your ID is {{shipper.ID}}.
                        Please drop us an email which includes a copy of your CV and the title following [ES-Shipper] Shipper *Your ID*.
                        Thanks for being a part of us.
                        Note: Your account is not activated yet. After reviewing your CV we will active your account once the requirement is met.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                </div>
            </div>

        </div>
    </div>
</div>
