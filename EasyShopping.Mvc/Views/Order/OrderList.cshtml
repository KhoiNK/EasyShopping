<div class="container" *ngIf="orders">
    <div class="row">
        <h2>Order List</h2>
    </div>
    <div class="row" *ngFor="let order of orders">
        <a [routerLink]="['order-detail', order.ID]">{{order.ID}}</a>
    </div>
</div>
